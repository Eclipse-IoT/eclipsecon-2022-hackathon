# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
name = "embedded-hal-async"
version = "0.1.0-alpha.1"
authors = ["The Embedded HAL Team and Contributors <embedded-hal@teams.rust-embedded.org>"]
description = "An asynchronous Hardware Abstraction Layer (HAL) for embedded systems"
documentation = "https://docs.rs/embedded-hal-async"
readme = "README.md"
keywords = [
    "hal",
    "IO",
]
categories = [
    "asynchronous",
    "embedded",
    "hardware-support",
    "no-std",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/rust-embedded/embedded-hal"

[dependencies.embedded-hal]
version = "=1.0.0-alpha.8"
